AI（Geminiなど）に「設計・テストケース作成・実装」をステップバイステップで確実に実行させるためのプロンプトテンプレートです。
このテンプレートは、AIが暴走して一気にコードを書きすぎるのを防ぎ、人間のレビューを強制挟む設計にしています。

---

## 📋 汎用プロンプトテンプレート

※ [ ] の部分を書き換えて、1つ目のプロンプトから順番にAIへ投入してください。

## 【ステップ1：インターフェース設計】

前提条件:
私は現在、[システム/アプリ名（例: ECサイトのバックエンド）] の開発を行っています。
モジュール単位での設計、テスト、実装を確実に進めるため、ステップバイステップで対応してください。まずは設計のみを行います。
タスク:
以下の要件を満たすモジュールの、インターフェース（関数・クラスのシグネチャ、引数、戻り値の型、発生しうる例外の定義）のみを作成してください。
※注意: 内部ロジックや具体的な実装コードは、まだ絶対に書かないでください。
対象モジュールの要件:

- 機能の概要: [例: ユーザーのパスワード強度のバリデーションを行う]
- 使用言語・フレームワーク: [例: Python 3.11, FastAPI]
- 入力値: [例: 文字列（パスワード）]
- 出力値: [例: Boolean（成否）と、エラーメッセージのリスト]

## 【ステップ2：テストケース設計（網羅性の担保）】

（ステップ1の出力に合意した後、続けて以下を投入）

タスク:
先ほど決定したインターフェースに基づき、テストコードを実装する前に、まずは「テストケースの仕様（一覧）」を出力してください。
以下の4つの観点を必ず網羅した箇条書きのリストにしてください。まだコード（テストコード含む）は書かないでください。

1.  正常系: 期待される一般的な入力と、その正しい出力結果
2.  境界値（エッジケース）: 最小値、最大値、空値、配列の要素数0/上限、Null/Noneなど
3.  異常系: 不正なデータ型、範囲外の数値、想定外のフォーマットなど
4.  例外処理: エラー発生時に期待通りの例外（Exception）が正しくスローされるか

(※ここでAIが出してきたリスト（ステップ3）を見て、人間が「〇〇のパターンも追加して」とレビュー・修正します)

## 【ステップ4：テストコードの生成】

（テストケースが確定した後、続けて以下を投入）

タスク:
確定したテストケースをすべて検証するための、具体的なテストコードのみを生成してください。
制約事項:

- テストフレームワーク: [例: PyTest / JUnit5 / Jest]
- [必要に応じて追加] 外部APIやDBとの通信が発生する部分は、[Mockライブラリ名] を使用して適切にモック化してください。

## 【ステップ5：実装コードの生成】

（テストコードが生成された後、最後に投入）

タスク:
先ほど作成したテストコードをすべてパス（Green）にできる、最もシンプルで堅牢な「実装コード」を記述してください。
適切なエラーハンドリングと、可読性の高いコードコメントも含めてください。

---

このテンプレートをコピペして使い回すことで、開発の脱線を防ぎ、常に品質が保証されたモジュールだけを量産できます。
まずは簡単なモジュールから試してみませんか？
よろしければ、このテンプレートの [ ] を埋めるための「今回作りたいモジュールの概要」や「プログラミング言語」を教えてください。その内容に合わせて最適化した最初のプロンプトを組み立てます。

それは素晴らしい仕組みですね！モジュールごとにREADMEを最新の状態に保つことは、後々の「階層的な結合」を行う際の最強の仕様書になります。結合を担当するAI（または人間）が、そのモジュールを正しく扱うための「取扱説明書」として機能するからです。
実装が終わった直後であれば、AIはコードの仕様を100%理解しているため、最も正確なドキュメントを作成できます。
開発プロセスの最後に組み込める「README自動生成プロンプト」を作成しました。ステップ5（実装完了）の直後にそのまま投入してご活用ください。

---

## 📋 【ステップ6：READMEの自動生成】プロンプト

タスク:
これまで作成したインターフェース、テスト、実装コードを基に、このモジュールのREADME.mdをMarkdown形式で出力してください。
後からこのモジュールを他のモジュールと結合（インテグレーション）する際に、開発者がこのREADMEだけを読めば正しく扱えるよう、以下の構成で記述してください。
構成案:

1.  概要 (Overview): このモジュールが提供する機能と目的
2.  インターフェース (API Reference):

- 関数/クラス名、引数（型と意味）、戻り値（型と意味）
  - 発生しうる例外（Exception）とその条件

3.  利用例 (Usage Example): 最も標準的なコードの呼び出し例（コードブロック）
4.  テストの実行方法 (Testing): このモジュールのテストを実行するためのコマンド
5.  注意点/制約事項 (Caveats): 依存関係や、利用上の制限（例: スレッドセーフかどうか、NULLの扱いなど）

---

## 💡 このステップを入れるメリット

- 結合フェーズの劇的な効率化: 上層のモジュールを作る際、このREADMEをAIにコンテキスト（前提知識）として渡すだけで、インターフェースのミスマッチがほぼゼロになります。
- ドキュメントの形骸化防止: コード変更と同時にドキュメントが更新されるため、「コードと仕様書がズレている」という開発あるあるを防げます。

この「設計 ➡️ テスト設計 ➡️ レビュー ➡️ テスト実装 ➡️ コード実装 ➡️ README作成」という6ステップのサイクルは、AI駆動開発における現時点で最も手戻りが少ない黄金律（ベストプラクティス）と言えます。
一連の流れを試してみて、もし「AIが途中で前のステップの内容を忘れてしまう（コンテキスト落ち）」や「READMEの記述が薄い」など、実際に運用する中で気になる点が出てきたらいつでも教えてください。さらに精度を高めるプロンプトの調整（出力フォーマットの固定化など）をお手伝いします。
